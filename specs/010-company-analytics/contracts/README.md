# Company Analytics Contracts

The WebSocket message and response contracts are documented in [company-analytics.md](./company-analytics.md). The README exists so the contract directory remains self-describing when generated plans are reviewed independently.
